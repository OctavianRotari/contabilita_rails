<%= form_for @category_of_company do |form| %>
		<% if @category_of_company.errors.any? %>
			<%= pluralize(@category_of_company.errors.count, "error") %> prevented your account from being created:
			<ul>
				<% @category_of_company.errors.full_messages.each do |error_message| %>
					<li><%= error_message %></li>
				<% end %>
			</ul>
	<% end %>
	<div>
		<%= form.label 'Categoria' %>
		<%= form.text_field :category, require: true %>
	</div>
	<br>
	<div>
		<%= form.submit "Aggiungi categoria" %>
	</div>
<% end %>
