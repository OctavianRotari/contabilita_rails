<div class="companies">
	<% @companies.each do |category_id, companies| %>
		<h2 style="margin-bottom: 0px"><%= category_of_company(category_id) %></h2>
		<%= link_to "Modifica categoria", edit_category_of_company_path(category_id)  %><br>
		<div style="display: inline-block;">
			<h4>Fatture passive</h4>
			<%byebug%>
			<p>da pagare: <%= @calculator.passive_amount_to_pay(companies) %> </p>
		</div>
		<div style="display: inline-block;">
			<h4>Fatture attive</h4>
			<p>da incassare: <%= @calculator.active_amount_to_collect(companies) %> </p>
		</div>
		<div style="display: inline-block;">
			<h4>Totale spese</h4>
			<p>Mese corrente: <%= @calculator.total_costs_current_month(category_id) %></p>
			<p>Anno corrente: <%= @calculator.total_costs_current_year(category_id) %></p>
		</div>
		<br>
		<br>
		<%= render companies %>
	<% end %>
</div>
