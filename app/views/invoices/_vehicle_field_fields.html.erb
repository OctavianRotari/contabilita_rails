<fieldset>
  <div class="row">
    <div class="col span-1-of-3">
      <%= f.label 'Mezzo' %>
    </div>
    <div class="col span-2-of-3">
      <%= f.select(:vehicle_id) do %>
        <%= options_from_collection_for_select(@vehicles, :id, :plate, selected: f.object.vehicle_id) %>
      <% end %>
    </div>
  </div>
  <div class="row">
    <div class="col span-1-of-3">
      <%= f.label 'Totale' %>
    </div>
    <div class="col span-2-of-3">
      <%= f.text_field :part_of_total %>
    </div>
    <%= f.hidden_field :_destroy %>
    <%= link_to "Remove", '#', class: "remove_fields" %>
  </div>
</fieldset>
