<% if vehicle.insurances.active %>
  <div class="col span-1-of-2 data">
    <p id='vehicle_plate'><%= link_to vehicle.plate, vehicle_path(vehicle.id, month: Time.zone.now.month, year: Time.zone.now.year) %></p>
  </div>
<% end %>
